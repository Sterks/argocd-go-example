apiVersion: v1
kind: Service
metadata:
  name: argocd-go-example
  namespace: argocd-go-example
  labels:
    app: argocd-go-example
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: argocd-go-example
