---
# LoadBalancer сервис для прямого доступа к Vault UI
# Этот сервис не управляется Helm и сохраняется независимо
# Используем порты 8200 и 8443 чтобы избежать конфликта с Traefik
apiVersion: v1
kind: Service
metadata:
  name: vault-lb
  namespace: vault
  labels:
    app: vault
    app.kubernetes.io/name: vault
    app.kubernetes.io/instance: vault
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  ports:
    - name: http
      port: 8200
      targetPort: 8200
      protocol: TCP
    - name: https
      port: 8443
      targetPort: 8200
      protocol: TCP
  selector:
    app.kubernetes.io/name: vault
    app.kubernetes.io/instance: vault
    component: server
