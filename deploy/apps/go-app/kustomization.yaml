apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd-go-example

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: argocd-go-example
