apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - namespace.yaml
  - argocd-service.yaml
  - argocd-certificate.yaml
  - argocd-ingress.yaml

commonLabels:
  app: argocd
