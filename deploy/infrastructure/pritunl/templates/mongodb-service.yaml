apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: pritunl
  labels:
    app: mongodb
spec:
  clusterIP: None
  ports:
  - port: 27017
    targetPort: 27017
    name: mongodb
  selector:
    app: mongodb
