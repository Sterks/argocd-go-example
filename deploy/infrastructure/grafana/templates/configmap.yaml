apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-custom-config
  namespace: victoriametrics
  labels:
    grafana_config: "true"
data:
  grafana.ini: |
    [server]
    domain = grafana.techbit.su
    root_url = http://grafana.techbit.su/
    serve_from_sub_path = false
